# LinearAlgebraCourse
This repository contains content for a course in linear algebra.

The content consists of Jupyter notebooks created in Visual Studio Code. The notebooks run in a conda environment. To set up the environment, use the provided `requirements.txt` file to install the necessary Python packages.
